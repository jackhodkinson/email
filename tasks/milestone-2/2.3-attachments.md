# Task 2.3: Attachments Display

**Milestone**: 2 - Read an Email
**Status**: Todo
**Dependencies**: 2.1 Email Detail Route
**Blocks**: None

## Goal

Display attachment list and enable downloading attachments.

## Context

- Attachment metadata stored in SQLite (Milestone 1)
- Attachment content fetched on-demand from Gmail API
- Need server function to fetch and return attachment data

## Requirements

### 1. Server Function for Attachment Download

```typescript
export const downloadAttachment = createServerFn({ method: "GET" })
  .validator((data: { emailId: string; attachmentId: string }) => data)
  .handler(async ({ data }) => {
    // Get account for this email
    // Fetch attachment via Gmail API
    // Return base64 data with mime type
  });
```

### 2. Attachments List Component

- Show filename, size, mime type icon
- Download button/link
- Handle multiple attachments

### 3. Download Handler

- Trigger browser download
- Show loading state during fetch

## Acceptance Criteria

- [ ] Attachments listed below email content
- [ ] Each shows filename and human-readable size
- [ ] Click downloads the file
- [ ] Loading indicator during download
- [ ] Works for common types (PDF, images, docs)

## Files to Create/Modify

| File | Action |
|------|--------|
| `email-app/src/components/attachment-list.tsx` | Create |
| `email-app/src/server/functions.ts` | Modify |
| `email-app/src/lib/db/queries.ts` | Modify (get attachments) |
